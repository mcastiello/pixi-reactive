import React from 'react';
import { Pages } from '../../pages';
import PageIndex from '../PageIndex';

const Contexts: React.FC = () => {
  return <PageIndex page={Pages.Contexts} />;
};

export default Contexts;
