import React from 'react';
import { Pages } from '../../pages';
import PageIndex from '../PageIndex';

const Graphics: React.FC = () => {
  return <PageIndex page={Pages.Graphics} />;
};

export default Graphics;
