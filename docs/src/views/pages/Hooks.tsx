import React from 'react';
import { Pages } from '../../pages';
import PageIndex from '../PageIndex';

const Hooks: React.FC = () => {
  return <PageIndex page={Pages.Hooks} />;
};

export default Hooks;
