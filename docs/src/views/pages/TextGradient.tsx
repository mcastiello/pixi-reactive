import React from 'react';
import EnumeratorList from '../EnumeratorList';
import { TextGradient } from 'pixi-reactive';

const TextGradientDoc: React.FC = () => {
  return <EnumeratorList name={'TextGradient'} enumerator={TextGradient} />;
};

export default TextGradientDoc;
