import React from 'react';
import EnumeratorList from '../EnumeratorList';
import { TextWhiteLine } from 'pixi-reactive';

const TextWhiteLineDoc: React.FC = () => {
  return <EnumeratorList name={'TextWhiteLine'} enumerator={TextWhiteLine} />;
};

export default TextWhiteLineDoc;
